<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Places</title>
  </head>
  <body>
    <form action="/" method="post">
      <label for="cityName">Enter a city:</label>
      <input id="cityName" name="cityName" type="text">
      <button type="submit">Submit</button>
    </form>

    {% if matchcities is not empty %}

      {% for matchcity in matchcities %}

      <li>City Name: {{ matchcity.getCityName }}</li>
      <li>Country:{{ matchcity.getCountry }}</li>
      <li>Stay Time: {{ matchcity.getstayLength }}</li>
      <li>Picture: {{ matchcity.getImage }}</li>

      {% endfor %}

      {% else %}
      <p>No match!</p>
    {% endif %}

  </body>
</html>
